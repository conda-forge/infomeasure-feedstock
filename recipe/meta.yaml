{% set name = "infomeasure" %}
{% set version = "0.6.3" %}
{% set python_min = "3.10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://files.pythonhosted.org/packages/source/{{ name[0] }}/{{ name }}/infomeasure-{{ version }}.tar.gz
  sha256: 2bbdfad86e23f91af788ee36f13d94b6de85c5105266a21247c87e91a96f64ea

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=61.2
    - pip
  run:
    - python >={{ python_min }}
    - numpy >=1.22
    - scipy >=1.0.0
    - sparse >0.10.0

test:
  requires:
    - python {{ python_min }}
    - pip
    - pytest
  imports:
    - infomeasure
  source_files:
    - tests
  commands:
    - pip check
    - pytest tests

about:
  summary: Comprehensive collection of information measures.
  description: |
    infomeasure in a Python package for calculating various information measures,
    including entropy, mutual information, transfer entropy, and more, with support
    for both discrete and continuous variables.
  home: https://infomeasure.readthedocs.io/
  license: AGPL-3.0-or-later
  license_file: LICENSE
  doc_url: https://infomeasure.readthedocs.io/
  dev_url: https://github.com/cbueth/infomeasure

extra:
  recipe-maintainers:
    - cbueth
